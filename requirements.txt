# Core dependencies
numpy
sounddevice
soundfile
Pillow  # Required by transformers/torchvision; also draws the Cast sidebar icons
av  # MP3/M4A export in the GUI (bundles its own encoders, no system ffmpeg needed)
sv-ttk  # ttk theme for the Studio UI

# PyTorch (install based on your CUDA version)
# For CUDA 11.8:
# torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1:
# torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CPU only:
# torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# Qwen TTS
qwen-tts

# Optional: Flash Attention (for faster GPU inference)
# flash-attn --no-build-isolation
